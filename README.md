download crouton
-----------------
enable developer mode on chromebook
download couton
open shell (ctrl+alt+t) `shell`
`crouton -t cli-tools -r trusty`
```
sudo enter-chroot

sudo apt-get update && sudo apt-get install build-essential curl git m4 ruby texinfo libbz2-dev libcurl4-openssl-dev libexpat-dev libncurses-dev zlib1g-dev nano
```

install hub
 - download, extract copy hub to ~/bin, make executable (chmod u+x ~/bin/hub)

install dotfiles and bootstrap them

